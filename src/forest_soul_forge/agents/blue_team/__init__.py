"""Aspirational placeholder — future blue-team agent factory.

Status: empty by design at v0.1. See parent
``forest_soul_forge.agents.__init__`` for the full rationale.

The blue-team naming anchors v0.3+ work on a defensive-agent factory
that wraps the existing Security Swarm role kits (ADR-0033) into
operator-friendly composition primitives. v0.1 ships those kits via
``config/tool_catalog.yaml::archetypes`` and operators use the
existing ``/birth`` + ``/spawn`` endpoints — no factory abstraction
needed yet.
"""
