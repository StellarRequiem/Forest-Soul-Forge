{
  "name": "forest-soul-forge-frontend",
  "version": "0.5.0",
  "private": true,
  "description": "SoulUX reference frontend for the Forest Soul Forge kernel. Vanilla JS, no build step. Per ADR-0044, this is part of the SoulUX flagship distribution; the kernel runs without it.",
  "type": "module",
  "scripts": {
    "test": "vitest run",
    "test:watch": "vitest",
    "test:coverage": "vitest run --coverage"
  },
  "devDependencies": {
    "vitest": "^1.6.0",
    "jsdom": "^24.0.0"
  }
}
