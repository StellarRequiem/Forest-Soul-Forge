# Invalid: entry_point missing sha256 — the trust boundary per spec §3.3.
# 'Loosening this is a breaking change' per spec §3.3.
schema_version: 1
name: example-no-sha
version: "1.0.0"
type: mcp_server
description: "entry_point.sha256 is required per spec §3.3 — the trust boundary."
side_effects: read_only
entry_point:
  type: stdio
  command: ["python", "-m", "example"]
